stages:
  - build
  - pages

default:
  image: node:20

build:
  stage: build
  variables:
    HUGO_BASEURL: $CI_PAGES_URL
  script:
    - npm ci
    - npm run docs-build

pages:
  stage: pages
  script:
    - cp _site public
    - echo "Deploying!"
  artifacts:
    paths:
      - public  